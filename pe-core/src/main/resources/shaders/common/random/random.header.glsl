/**
*
* Random Header file
*
**/

float random1f(float seed);
float random1f(vec2 seed);
float random1f(vec3 seed);
float random1f(vec4 seed);

vec2 random2f(float seed);
vec2 random2f(vec2 seed);
vec2 random2f(vec3 seed);
vec2 random2f(vec4 seed);

vec3 random3f(float seed);
vec3 random3f(vec2 seed);
vec3 random3f(vec3 seed);
vec3 random3f(vec4 seed);

vec4 random4f(float seed);
vec4 random4f(vec2 seed);
vec4 random4f(vec3 seed);
vec4 random4f(vec4 seed);

float goldenNoise(vec2 xy, float seed);